# Chess and engine
python-chess>=1.999

# Stockfish: install separately (e.g. apt install stockfish). Set STOCKFISH_PATH in .env or config if not in PATH.

# Template vision: board extraction + FEN from screenshot
numpy>=1.24.0
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# Assist mode: screen capture
mss>=9.0.0

# Config
python-dotenv>=1.0.0
pyyaml>=6.0
